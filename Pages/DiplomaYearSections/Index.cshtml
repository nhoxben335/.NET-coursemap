@page
@model nscccoursemap_nhoxben335.Pages.DiplomaYearSections.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Diploma Year Section</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DiplomaYearSection[0].DiplomaYear)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DiplomaYearSection[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DiplomaYearSection[0].AcademicYear)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DiplomaYearSection) {
        <tr>
            <td>
@Html.DisplayFor(modelItem => item.DiplomaYear.Diploma.Title) - @Html.DisplayFor(modelItem => item.DiplomaYear.Title)            </td>
           <td>
                @Html.DisplayFor(modelItem => item.DiplomaYear.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AcademicYear.Title)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
